#!/usr/bin/env node

import yargs from 'yargs';
import { hideBin } from 'yargs/helpers';
/*
import ScaffoldCommand from './command/command.schema.builder';

const cliAdapter = yargs(hideBin(process.argv))
  .scriptName(`react-nucleus`)
  .usage(`$0 <cmd> [args]`);

const scaffoldCommand = new ScaffoldCommand(cliAdapter);

cliAdapter
  .command(scaffoldCommand)
  .demandCommand(1)
  .help()
  .alias(`h`, `help`)
  .parse();
*/
